import { test, expect } from '@playwright/test';
// const process = require('process');


test('Repository Creation (system admin)', async ({page}) => {
    // 1. Login DONE 
    // 2. Hit Administration tab on bottom of screen
    // 3. Manage Data Repositories button
    // 4. New button
    // 5. Enter the following fields:
    // ID, Title, Srvr. Address, Public Key, Path, Capacity
    // 6. Add an admin to the repo
    // 7. Enter Save


});