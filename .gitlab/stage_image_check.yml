---

include:
  - local: .gitlab/common.yml

check-ws-image:
  extends: .image_check
  stage: image-check
  variables:
    PROJECT: "datafed"
    COMPONENT: "ws"

check-core-image:
  extends: .image_check
  stage: image-check
  variables:
    PROJECT: "datafed"
    COMPONENT: "core"

check-repo-image:
  extends: .image_check
  stage: image-check
  variables:
    PROJECT: "datafed"
    COMPONENT: "repo"

check-gcs-base-image:
  extends: .image_check
  stage: image-check
  variables:
    PROJECT: "datafed"
    COMPONENT: "gcs_base"

check-gcs-image:
  extends: .image_check
  stage: image-check
  variables:
    PROJECT: "datafed"
    COMPONENT: "gcs"

check-foxx-image:
  extends: .image_check
  stage: image-check
  variables:
    PROJECT: "datafed"
    COMPONENT: "foxx"
