# Globus Connect Server Environment Variables Template
# Copy this file to .env and fill in your values

# ===== REQUIRED CONFIGURATION =====
# Hostname for the GCS endpoint (must be resolvable)
GCS_HOSTNAME=your-gcs-hostname.example.com
# Public IP address of the GCS endpoint
GCS_IP_ADDRESS=1.2.3.4
# Display name prefix for Globus resources
GCS_ROOT_NAME=ROVI GCS
# Collection name
GCS_COLLECTION_NAME=ROVI Collection
# Base path for collections on the host filesystem
GCS_COLLECTION_ROOT_PATH=/mnt/globus-collections
# Host path that will be mounted into container
HOST_COLLECTION_PATH=/path/to/host/storage

# Globus OAuth2 Application credentials
# Create at https://app.globus.org/settings/developers
GLOBUS_CLIENT_ID=your-client-id
GLOBUS_CLIENT_SECRET=your-client-secret

# Globus subscription ID (required for guest collections)
GLOBUS_SUBSCRIPTION_ID=your-subscription-id

# ===== OPTIONAL CONFIGURATION =====
# Control port for GCS (default: 443)
GCS_CONTROL_PORT=443
# Allowed authentication domains (comma-separated)
# Example: ornl.gov,globusid.org
GCS_ALLOWED_DOMAINS=ornl.gov
# Organization name
GCS_ORGANIZATION=Oak Ridge National Laboratory
# Contact email for the endpoint
GCS_CONTACT_EMAIL=admin@example.com
# Local user for file ownership
LOCAL_USER=globus
# UID for the local user (should match host filesystem permissions)
UID=1000