syntax = "proto3";

package SDMS;

option cc_enable_arenas = true;

enum SortOption {
    SORT_ID = 0;
    SORT_TITLE = 1;
    SORT_OWNER = 2;
    SORT_TIME_CREATE = 3;
    SORT_TIME_UPDATE = 4;
    SORT_RELEVANCE = 5;
}
