syntax = "proto3";

package sdms;

option cc_enable_arenas = true;

message RepoAllocationSetRequest {
    string repo = 1;
    string subject = 2;
    uint64 data_limit = 3;
    uint32 rec_limit = 4;
}
