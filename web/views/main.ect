<!DOCTYPE html>
<html>
    <head>
        <% include 'head.ect' %>
        <script type="text/javascript">
            loadUser();
            if ( !g_user || !g_user.active )
                window.location.replace("/");
        </script>
        <title>SDMS Main</title>
    </head>
    <body>
    <div class="col-flex">
        <div class="header" style="flex:none">
            <div class="row-flex">
                <div style="flex:1;font-size: 1.6em">Scientific Data Management System</div><div style="flex:none;display:flex;justify-content:flex-end"><button class="button" onclick="logout()">Log Out</button></div>
            </div>
        </div>
        <div class="content" style="flex:1">
            <p>This is the main SDMS page for authenticated users.</p>
        </div>
        <div class="footer" style="flex:none">
            User: <span id=uname></span>
        </div>
    </div>
    </body>
    <script>
        document.getElementById("uname").innerHTML = g_user.name;
    </script>
</html>
