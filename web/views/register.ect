<!DOCTYPE html>
<html>
    <head>
        <% include 'head.ect' %>
        <title>SDMS Register</title>
    </head>
    <body>
        <div class="col-flex" style="height:100%">
            <div class="header" style="flex:none">
                <div class="row-flex">
                    <div style="flex:1;font-size: 2em">Scientific Data Management System - Registration</div><div style="flex:none;display:flex;justify-content:flex-end"><button class="btn" onclick="location.href='/ui'">Home</button></div>
                </div>
            </div>
            <div id="content" class="ui-widget-content" style="padding:5em;flex:1 1 100%">
                <p>TO DO: Restrict to specific organizations, projects, and/or staff groups.</p>
                <p>Please enter a password for your new SDMS account. This password is needed for the SDMS command-line interface (CLI). The SDMS CLI can be used to generate local credentials to support automatic authentication.</p>
                <br>
                <table style="margin:1em auto">
                <tr><td>New Password:</td><td><input type="password" id="pw1"></td></tr>
                <tr><td>Re-enter Password:</td><td><input type="password" id="pw2"></td></tr>
                <tr><td>&nbsp</td><td> </td></tr>
                <tr><td></td><td><button class="btn" onclick="register()" type="button">Register</button></td></tr>
                </table>
            </div>
            <div class="footer" style="flex:none">
                User: Not Registered
            </div>
        </div>
    </body>
    <script>
        $(".btn").button();

        function register() {
            var pw1 = document.getElementById('pw1').value;
            var pw2 = document.getElementById('pw2').value;
            if ( pw1.length == 0 )
                alert( "Password cannot be empty" );
            else if ( pw1 != pw2 )
                alert( "Passwords do not match" );
            else
                window.location = "/ui/do_register?pw="+pw1+"&acc_tok=<%- @acc_tok %>&ref_tok=<%- @ref_tok %>";
        }
    </script>
</html>
